<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/main_sub_header_color">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="6dp">

        <EditText
            android:id="@+id/search_bar"
            android:layout_width="282dp"
            android:layout_height="wrap_content"
            android:drawableLeft="@drawable/search_icon"
            android:hint="Search" />

        <ImageButton
            android:id="@+id/select_all_btn"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginRight="13dp"
            android:layout_toLeftOf="@id/add_playList_button"
            android:backgroundTint="@color/main_btn_color"
            android:src="@drawable/select_all_white" />

        <ImageButton
            android:id="@+id/add_playList_button"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_alignParentRight="true"
            android:layout_marginRight="11dp"
            android:backgroundTint="@color/main_btn_color"
            android:src="@drawable/add_cross"
            android:onClick="add_playList_btnClick"/>

    </RelativeLayout>



</RelativeLayout>